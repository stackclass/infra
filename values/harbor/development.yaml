expose:
  type: ingress
  tls:
    enabled: false
  ingress:
    hosts:
      core: docker.stackclass.local

externalURL: http://docker.stackclass.local

harborAdminPassword: "Harbor12345"

trivy:
  enabled: false

database:
  type: internal
  internal:
    password: "changeit"

redis:
  type: internal
