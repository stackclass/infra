ingress:
  enabled: true
  hosts:
    - host: repo.stackclass.local
      paths:
        - path: /

gitea:
  admin:
    username: stackclass
    password: r8sA8CPHD9!bt6d
    email: "admin@stackclass.dev"
    passwordMode: initialOnlyNoReset

valkey-cluster:
  enabled: false

valkey:
  enabled: true
  global:
    valkey:
      password: changeme

postgresql-ha:
  enabled: false

postgresql:
  enabled: true
  global:
    postgresql:
      auth:
        database: gitea
        username: gitea
        password: gitea
